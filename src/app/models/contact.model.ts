export class Contact {
    // Seguramente necesitemos un ID
    name: string;
    phone: number;  

    constructor(name: string, phone: number) {
        this.name = name;
        this.phone = phone;
    }
} 